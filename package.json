{
  "name": "tcc",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "build": "bun install --cwd frontend && bun run --cwd frontend build",
    "start": "bun run --cwd backend start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
